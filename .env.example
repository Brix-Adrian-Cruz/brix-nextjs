# Copy to .env.local and fill in. .env.local is gitignored.

# Your WPGraphQL endpoint — the WordPress URL plus /graphql.
# Confirm the exact path under GraphQL -> Settings in wp-admin.
# Set this per environment on Pantheon (Test and Live may differ).
WORDPRESS_API_URL=https://live-your-site.pantheonsite.io/graphql
